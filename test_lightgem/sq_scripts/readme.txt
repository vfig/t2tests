Trying to use the vismeter_ang config to rotate the lightgem (so it could be a combined lightgem and compass perhaps!).

But the config isn't re-read unless the game mode changes (e.g. go to map and back out).

So this is no good.